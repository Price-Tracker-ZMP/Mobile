<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:PriceTrackerMobile.ViewModels"
             xmlns:model="clr-namespace:PriceTrackerMobile.Models"
             x:Class="PriceTrackerMobile.Views.PriceDetailPage"
             x:Name="DetailPage">
    <ContentPage.BindingContext>
        <viewmodels:PriceDetailViewModel/>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout>
            <Label 
                Text="{Binding DetailedGame.Name}" />
            <Image 
                Source="{Binding DetailedGame.ImageUrl}" />
            <Label 
                Text="{Binding DetailedGame.Id}" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>