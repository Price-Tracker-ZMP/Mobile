<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:PriceTrackerMobile.ViewModels"
             x:Class="PriceTrackerMobile.Views.AddItemPage"
             Shell.PresentationMode="Modal">
    <ContentPage.BindingContext>
        <viewmodels:AddItemViewModel />
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Welcome to adding page!"
                VerticalOptions="CenterAndExpand" 
                HorizontalOptions="CenterAndExpand" />
            <Entry 
                Placeholder="Full steam game url"
                PlaceholderColor="Red"
                Text="{Binding gameUrl}"/>
            <Label 
                FontSize="Medium"
                Text="Paste Steam game url that u wanna track and click done"
                VerticalOptions="Center" 
                HorizontalOptions="CenterAndExpand" />
            <Button Text="Browse" Command="{Binding BrowseCommand}"/>
            <Label 
                FontSize="Small"
                Text="Browse button direct you to steam website"
                VerticalOptions="Center" 
                HorizontalOptions="CenterAndExpand" />
            <Button Text="Done" Command="{Binding AddCommand}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>